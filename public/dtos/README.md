`public/dtos` should contain all the public data transfer objects (structs) which is used to communicate with other web services via APIs.

These DTOs can be accessed and vectorized via external service.